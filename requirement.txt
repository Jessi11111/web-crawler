1.After updating the requirements.txt file, run the following command to install all the dependencies listed in the file:
pip install -r requirements.txt

2. eg: Verify Installation: Once the installation is complete, verify that selenium and webdriver-manager are installed:
pip show selenium
pip show webdriver-manager
